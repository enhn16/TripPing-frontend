# Existing backend origin. Leave empty only when /api is reverse-proxied on the same origin.
VITE_API_BASE_URL=https://api.example.com
# Public Kakao JavaScript key (never a client secret).
VITE_KAKAO_MAP_KEY=
# Local development only; production always uses real authentication.
VITE_DEV_FORCE_LOGIN=false
